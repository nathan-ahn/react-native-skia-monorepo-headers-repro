{
	"name": "expo-app",
	"version": "1.0.0",
	"scripts": {
		"start": "bunx expo start --dev-client",
		"ios": "expo run:ios",
		"android": "expo run:android",
		"rebuild": "rm -rf ios android && bunx expo prebuild"
	},
	"expo": {
		"autolinking": {
			"searchPaths": [
				"../packages",
				"../node_modules",
				"../../node_modules"
			],
			"nativeModulesDir": "../packages"
		}
	},
	"dependencies": {
		"expo": "~54.0.0",
		"expo-build-properties": "~1.0.0",
		"expo-dev-client": "~6.0.0",
		"react": "~19.1.0",
		"react-native": "0.81.4",
		"src": "workspace:*"
	}
}
